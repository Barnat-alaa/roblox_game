--!strict
-- Café visits + safe predefined reactions — Week 4. Placeholder for now.
-- Will validate VisitCafe requests and enforce the "no free-text" rule.

local SocialService = {}
SocialService.Name = "SocialService"

function SocialService.Init(_self: any) end
function SocialService.Start(_self: any) end

return SocialService
