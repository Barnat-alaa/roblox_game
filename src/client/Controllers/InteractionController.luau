--!strict
-- Appliance proximity prompts + "walk up to the counter to serve" flow — Day 3.
-- Placeholder so the controller framework is complete; the serve action lives on
-- the HUD button for now.

local InteractionController = {}
InteractionController.Name = "InteractionController"

function InteractionController.Start(_self: any) end

return InteractionController
